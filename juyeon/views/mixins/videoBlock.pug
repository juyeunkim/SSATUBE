mixin videoBlock(video = {})
    .videoBlock
        video.videoBlock__thumbnail(src=video.videoFile, controls=true) // controls= true : 동영상 자동 재생
        h4.videoBlock__title=video.title
        h6.videoBlock__views=video.views 